# Upstream IDA Pro MCP Reference

This directory contains a snapshot of the previous upstream MCP implementation kept only as implementation reference during the rewrite.

Do not import code from this directory at runtime. New runtime code belongs in:

- `ida-plugin/` for IDA Python plugin code
- `mcp-center/` for .NET center application code
