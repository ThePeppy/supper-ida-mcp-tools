[project]
name = "supper-ida-plugin"
version = "0.1.2"
description = "IDA Pro executor plugin for Supper IDA MCP Tools"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
